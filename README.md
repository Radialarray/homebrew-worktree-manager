# Homebrew Worktree Manager

This tap provides the `worktree-manager` formula for managing git worktrees.

## Installation

```bash
brew tap radialarray/worktree-manager
brew install worktree-manager
```

## Usage

See [worktree-manager documentation](https://github.com/Radialarray/worktree-manager) for usage instructions.

